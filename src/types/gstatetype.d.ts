// types/gstatetype.d.ts

export interface GStateOptions {
    opacity?: number;
    // Add other properties if needed
  }
  
  export declare class GState {
    constructor(options: GStateOptions);
  }